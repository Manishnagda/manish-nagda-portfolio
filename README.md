# Manish Nagda — Portfolio

A responsive personal portfolio built with React and Vite.

## Run locally

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
npm run preview
```

## Deploy to Vercel

Import this repository on Vercel. The framework preset is detected as Vite; use `npm run build` and the `dist` output directory.
